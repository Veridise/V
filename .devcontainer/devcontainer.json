{
    "name": "V",
    "build": {
        "dockerfile": "../Dockerfile",
		    "context": ".."
    },
    "settings": {
		    "terminal.integrated.profiles.linux": {
			      "bash": {
				        "path": "/bin/bash"
			      }
		    }
	  }
}
